[build-system]
requires = ["uv_build>=0.9.6,<0.10.0"]
build-backend = "uv_build"

[project]
name = "knowledge-assistant"
version = "0.0.1+gce7f286"
requires-python = ">=3.12,<3.13"
description = "RAG-style Knowledge Assistant demo brick for RaceTrac."
dependencies = ["common @ file://${PROJECT_ROOT}/../common"]

[tool.uv.sources]

[tool.uv.sources.common]
workspace = true
